const chalk = require('chalk');

module.exports = async function(options) {
  console.log(chalk.blue('🚀 Starting claude-code-keepalive...'));
  console.log(chalk.yellow('⚠️  Implementation coming soon'));
  console.log('Options:', options);
};